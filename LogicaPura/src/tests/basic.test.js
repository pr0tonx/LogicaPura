import { describe, it, expect } from 'vitest'

describe('basic test', () => {
    it('should pass', () => {
        expect(1 + 1).toBe(2)
    })

    it('should pass as well', () => {
        expect(1 + 1).toBe(2)
    })
})